# Web + server
flask
gunicorn
python-dotenv
requests
pillow

# ML stack (CPU wheels)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.3.1
torchvision==0.18.1
timm
openai

# XAI / Vision utils
grad-cam==1.4.8            # <— use this package name (imports as pytorch_grad_cam)
opencv-python-headless>=4.8

# Data science
numpy
scikit-learn
matplotlib
pandas

# Optional
ultralytics>=8.2.0

# If you need a CUDA build of PyTorch, install it separately, e.g.:
# pip install --index-url https://download.pytorch.org/whl/cu121 torch torchvision
