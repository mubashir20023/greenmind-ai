# Web + server
flask
gunicorn
python-dotenv
requests
pillow

# ML stack (CPU wheels)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.3.1
torchvision==0.18.1
timm

# XAI / Vision utils
pytorch-grad-cam==1.4.8
opencv-python-headless>=4.8

# Data science (unpin unless you must)
numpy
scikit-learn
matplotlib
pandas

# Optional (your repo uses it)
ultralytics>=8.2.0

# If you need a CUDA build of PyTorch, install it separately, e.g.:
# pip install --index-url https://download.pytorch.org/whl/cu121 torch torchvision
