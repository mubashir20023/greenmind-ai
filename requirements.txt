# Web + server
flask
gunicorn
python-dotenv
requests
pillow

# ML stack (CPU wheels)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.5.1
torchvision==0.20.1
openai

# XAI / Vision utils
grad-cam==1.5.5
opencv-python==4.*         # use non-headless to match your local
ultralytics==8.3.187

# Data science
numpy
scikit-learn
matplotlib
pandas

# Optional
ultralytics>=8.2.0

# If you need a CUDA build of PyTorch, install it separately, e.g.:
# pip install --index-url https://download.pytorch.org/whl/cu121 torch torchvision
open-clip-torch==3.1.0
faiss-cpu==1.12.0
timm==1.0.19
charset-normalizer>=3.0.0
chardet>=5.0.0